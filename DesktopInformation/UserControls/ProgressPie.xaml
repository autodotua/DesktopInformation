<UserControl x:Class="DesktopInformation.UserControls.ProgressPie"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DesktopInformation.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <local:ValueToProcessConverter x:Key="ValueToProcessConverter"/>
    </UserControl.Resources>
    <Grid>
        <ProgressBar Minimum="0" x:Name="p"
                     Maximum="1">
            <ProgressBar.Template>
                <ControlTemplate TargetType="ProgressBar">
                    <Border Background="{TemplateBinding Value, Converter={StaticResource ValueToProcessConverter}, ConverterParameter=10}"/>
                </ControlTemplate>
            </ProgressBar.Template>
        </ProgressBar>
    </Grid>
</UserControl>
